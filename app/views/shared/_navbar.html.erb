<nav class="nav">
  <div class="part-left">
    <% if user_signed_in? %>
      <%= link_to 'Todo List', lists_path %>
      <%= link_to 'New List', new_list_path %>
      <%= link_to 'Daily TODOs', user_daily_items_path %>
    <% else %>
      <%= link_to 'Todo List', root_path %>
    <% end %>
  </div>

  <div class="part-right">
    <% if user_signed_in? %>
      <p>
        <%= current_user.email %>
      </p>
      <%= link_to "Log out", destroy_user_session_path %>
    <% else %>
      <%= link_to "Log in", user_session_path %>
    <% end %>
  </div>
</nav>
